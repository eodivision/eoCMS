<?php
/*
$tempcon = mssql_connect({HOST},{USER},{PASS});
if($tempcon){
	$tempver = mssql_query('SELECT @@VERSION');
	$temprow = mssql_fetch_array($tempver);
	$version[] = "server version: ".$temprow[0];
}
*/
?>