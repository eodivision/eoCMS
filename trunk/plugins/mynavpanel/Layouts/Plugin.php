<?php

#DEV: pages may need renaming

# nothing yet, maybe:
#  write static
#  user lists (whos using it)
#  rank counts (if rank==clicks )
#  allow sharing (other groups or users can see it, see below)

# need to add old code again, My Navigation Panel can allow multiple panels per layout.. (look into this, may need another table)

?>