<?php
/*

define('DB_HOST', {HOST});
define('DB_USER', {USER});
define('DB_PASS', {PASS});
define('DB_NAME', {NAME});

$con = mysql_connect(DB_HOST, DB_USER, DB_PASS);
if(!$con)
	die('eoCMS was unable to connect to the database');

$select = mysql_select_db(DB_NAME);
if(!$select)
	die('eoCMS was unable to select the database '.DB_NAME);

*/
?>